// For Loop
// Help you to repeat a block of code.

// Increment Operator


//------------Pre Incremental---------
// ++   -> it increase the value first 
let a1 = 11;
let b1 = ++a1;
console.log(b1); ///// but its print same 11 => ( b1 = 1 + 11) = 12
console.log(a1);// a1 = 12 -- first increase the value then assign




///-------------Post Incremental --- a++ -- First assign the a value then add

let a = 10;   // a = a + 1;  //a++ also we can write
let b = a++;
console.log(b); // its print 10 -> (b = a) first =
console.log(a);// but its print 11 -> a++ = (a + 1) = 11



